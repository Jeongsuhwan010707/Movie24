spring.config.import=optional:application-local.properties

server.port=8082

spring.datasource.url=jdbc:mysql://localhost:3306/movie24?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 기존에 ddl-auto=update로 만들어져 있던 스키마를 V1 베이스라인으로 인정하고,
# V2부터의 마이그레이션만 실제로 적용한다.
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1

# 카카오 로그인: client-id/client-secret은 비밀값이라 application-local.properties에만 둔다.
spring.security.oauth2.client.registration.kakao.client-authentication-method=client_secret_post
spring.security.oauth2.client.registration.kakao.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.kakao.redirect-uri={baseUrl}/login/oauth2/code/kakao
spring.security.oauth2.client.registration.kakao.scope=profile_nickname,account_email
spring.security.oauth2.client.registration.kakao.client-name=Kakao

spring.security.oauth2.client.provider.kakao.authorization-uri=https://kauth.kakao.com/oauth/authorize
spring.security.oauth2.client.provider.kakao.token-uri=https://kauth.kakao.com/oauth/token
spring.security.oauth2.client.provider.kakao.user-info-uri=https://kapi.kakao.com/v2/user/me
spring.security.oauth2.client.provider.kakao.user-name-attribute=id

# 네이버 로그인: client-id/client-secret은 비밀값이라 application-local.properties에만 둔다.
# 네이버는 localhost를 서비스 URL로 허용하지 않아 127.0.0.1로 등록했다 - 로컬 테스트도 127.0.0.1로 접속해야 한다.
spring.security.oauth2.client.registration.naver.client-authentication-method=client_secret_post
spring.security.oauth2.client.registration.naver.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.naver.redirect-uri={baseUrl}/login/oauth2/code/naver
spring.security.oauth2.client.registration.naver.scope=name,email
spring.security.oauth2.client.registration.naver.client-name=Naver

spring.security.oauth2.client.provider.naver.authorization-uri=https://nid.naver.com/oauth2.0/authorize
spring.security.oauth2.client.provider.naver.token-uri=https://nid.naver.com/oauth2.0/token
spring.security.oauth2.client.provider.naver.user-info-uri=https://openapi.naver.com/v1/nid/me
spring.security.oauth2.client.provider.naver.user-name-attribute=response

# 구글 로그인: client-id/client-secret은 비밀값이라 application-local.properties에만 둔다.
# 기본 scope에 openid가 포함되면 스프링 시큐리티가 OIDC 로그인으로 처리해 CustomOAuth2UserService(순수 OAuth2용)를
# 타지 않고 내장 OidcUserService를 쓰게 되어 DB 연동이 안 된다. openid를 빼서 카카오/네이버와 같은 OAuth2 플로우로 통일한다.
spring.security.oauth2.client.registration.google.scope=email,profile
